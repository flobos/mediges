<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mediges.list">
      <field name="name">mediges list</field>
      <field name="model">mediges.mediges</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mediges.action_window">
      <field name="name">mediges window</field>
      <field name="res_model">mediges.mediges</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mediges.action_server">
      <field name="name">mediges server</field>
      <field name="model_id" ref="model_mediges_mediges"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mediges" id="mediges.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mediges.menu_1" parent="mediges.menu_root"/>
    <menuitem name="Menu 2" id="mediges.menu_2" parent="mediges.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mediges.menu_1_list" parent="mediges.menu_1"
              action="mediges.action_window"/>
    <menuitem name="Server to list" id="mediges" parent="mediges.menu_2"
              action="mediges.action_server"/>
    -->
  </data>
</odoo>